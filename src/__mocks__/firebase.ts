// Jest mock for ../firebase, used so tests don't initialize a real Firebase
// app (which requires a valid configuration) just by importing
// netplayClient.ts.

export const auth = {};

export const database = {};
